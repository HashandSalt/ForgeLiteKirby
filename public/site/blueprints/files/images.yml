title:                 Images

accept:                image/*

tabs:

  # BASIC
  basic:
    label:             Basic
    icon:              text
    columns:

      # LEFT COLUMN
      left:
        width:         2/3
        sections:
          meta:
            type:      fields
            fields:
              alt:
                label: Alt
                type:  text
              caption:
                label: Caption
                type:  textarea

      # RIGHT COLUMN
      right:
        width:         1/3
        sections:
          feature:
            type:      fields
            fields:
              feature:
                label: Feature Image?
                type:  toggle
                width: 1/3
